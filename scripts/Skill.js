document.addEventListener("DOMContentLoaded", () => {
    console.log("LearnOnix Skill Page Loaded Successfully ✅");

    // Optional: Add an interactive message or animation
    const btn = document.querySelector(".button");
    btn.addEventListener("click", () => {
        alert("Redirecting to the LearnOnix Telegram group!");
    });
});
